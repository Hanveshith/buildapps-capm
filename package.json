{
  "name": "LeaveManagement",
  "version": "1.0.0",
  "description": "A simple CAP project",
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": "^9",
    "@sap/xssec": "^4",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/cds-test": "^0",
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9"
  },
  "scripts": {
    "start": "cds-serve",
    "test": "node --test"
  },
  "private": true,
  "cds": {
    "requires": {
      "auth": {
        "kind": "basic",
        "users": {
          "sai@gmail.com": {
            "password": "sai",
            "roles": ["Employee"]
          },
          "manager@gmail.com": {
            "password": "manager",
            "roles": ["Manager"]
          }
        }
      },
      "[production]": {
        "auth": {
          "kind": "xsuaa"
        }
      }
    }
  }
}
